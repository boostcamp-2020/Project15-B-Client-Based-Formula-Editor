import { createSlice } from "@reduxjs/toolkit";

const { reducer, actions } = createSlice({
	name: "FEditor",
	initialState: {

	},
	reducers: {

	},
});

export const {

} = actions;

export default reducer;
